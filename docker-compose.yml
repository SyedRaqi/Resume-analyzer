services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: smarthire
      POSTGRES_USER: smarthire
      POSTGRES_PASSWORD: smarthire_dev
    ports:
      - "5432:5432"
    volumes:
      - smarthire_pgdata:/var/lib/postgresql/data
volumes:
  smarthire_pgdata:
